Algoritmo "ContarDe0Ate100"

Var

   contador: Inteiro

Inicio

      contador <- 0
      Enquanto (contador <= 100) faca
               EscrevaL (contador)
               contador <- contador + 1
      FimEnquanto
      EscrevaL ("Contagem Encerrada!")

Fimalgoritmo