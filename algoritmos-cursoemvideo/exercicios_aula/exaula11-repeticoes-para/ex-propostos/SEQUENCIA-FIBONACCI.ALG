Algoritmo "sequencia-fibonacci"

Var

   C, A, B, prox: Inteiro

Inicio

      A <- 0
      B <- 1
      Escreval (C)
      Para C <- 1 ate 14 faca
           Escreval (prox)
           prox <- A + B
           A <- B
           B <- prox
      FimPara

Fimalgoritmo